{# {% extends 'base.html.twig' %} #}
<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>PhoneFactory</title>
  {# BOOTSTRAP #}
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-4bw+/aepP/YC94hEpVNVgiZdgIC5+VKNBQNGCHeKRQN+PtmoHDEXuppvnDJzQIu9" crossorigin="anonymous">
  {# BOOTSTRAP #}

</head>


{% block title %}{% endblock %}



{% block body %}












<div class="b-example-divider mb-4"></div>

<div class="container col-xl-10 col-xxl-8 px-4 py-5 w-100">
  <div class="row align-items-center g-lg-5 py-5">

    <div class="col-md-10 mx-auto col-lg-5">
      <form method="post" class="p-4 p-md-5 border rounded-3 bg-body-tertiary">

        {% if error %}
        <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
        {% endif %}

        <body class="d-flex h-100 text-center text-bg-dark">
          <div class="form-floating mb-3">

            <input type="email" value="{{ last_username }}" name="email" id="inputEmail" class="form-control"
              autocomplete="email" required autofocus>
            <label for="inputEmail">Email address</label>
          </div>
          <div class="form-floating mb-3">
            <input type="password" name="password" id="inputPassword" class="form-control"
              autocomplete="current-password" required>
            <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">
            <label for="inputPassword">Password</label>
          </div>
          <div class="checkbox mb-3">

            <input class="form-check-input" type="checkbox" value="remember-me" id="flexCheckDefault" name="_remember_me">
            <label class="form-check-label text-light-emphasis mx-3" for="flexCheckDefault">
              Remember me
            </label>
          </div>
          
          <button class="btn btn-primary w-100 py-2 mb-2" type="submit">Sign in</button>
          <small class="text-body-secondary">By clicking Sign up, you agree to the terms of use.</small>
          <hr class="my-4">
          <p class="my-3 text-body-secondary">If you don't have an Account, Please <a
              href="{{ path('app_register') }}">Register</a> !</p>
      </form>
    </div>
    <div class="col-lg-7 text-center text-lg-start my-3">
      <h1 class="display-4 fw-bold lh-1 text-primary mx-2 my-3">PhoneFactory</h1>
      <p class="col-lg-10 fs-4 mx-2">Discover a curated collection of the latest Smartphone models. From stunning
        displays that bring your content to life with <span class="fst-italic">powerful processors</span>, that
        effortlessly handle multitasking.</p>
        {# <div class="box-2 d-flex justifiy-content-center">
        <form action="{{ path('app_login') }}" method="POST" class="d-flex row-md-auto">
          <input type="email" name="email0" class="form-control d-flex mx-2 row-md-auto">
          <button class="btn btn-outline-primary col-md-auto" type="submit">Send Login Link</button>
        </form></div> #}
    </div>
  </div>
</div>

</body>

</html>

{# OLD ONE #}
{# <div class="container w-50 my-5">
  <main class="form-signin w-100 m-auto">
    <form method="post">

      <h1 class="h3 mb-3 fw-normal mb-5">Please sign in</h1>

      <div class="form-floating">
        <input type="email" value="{{ last_username }}" name="email" id="inputEmail" class="form-control"
          autocomplete="email" required autofocus>
        <label for="inputEmail">Email address</label>
      </div>
      <div class="form-floating">
        <input type="password" name="password" id="inputPassword" class="form-control" autocomplete="current-password"
          required>
        <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">
        <label for="inputPassword">Password</label>
      </div>

      <div class="form-check text-start my-3">
        <input class="form-check-input" type="checkbox" value="remember-me" id="flexCheckDefault">
        <label class="form-check-label" for="flexCheckDefault">
          Remember me
        </label>
      </div>
      <button class="btn btn-primary w-25 py-2" type="submit">Sign in</button>
      <p class="my-3">If you don't have an Account, Please <a href="{{ path('app_register') }}">Register</a> !</p>

    </form>
  </main>



</div> #}

{% endblock %}